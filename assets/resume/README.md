# Résumé

Add the public PDF here with this exact filename:

`Akshat-Sharma-Resume.pdf`

The site links to `assets/resume/Akshat-Sharma-Resume.pdf`. GitHub Pages serves files in this folder directly, so a Google Drive link is not needed.

To publish a revised résumé, replace the PDF and commit the change. Keep the filename the same unless you also update both résumé links in `index.html`.
